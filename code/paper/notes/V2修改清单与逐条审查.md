# V2 修改清单与逐条审查（内部）

审查对象：`D:/EdgesDownloads/2026华为杯A题完整论文V1_修改提示词.md`（SHA-256：`0BD40A0DB7BDF1FF3A80FE50536429329DA105F6602967D4D6289EF3C6E1AD38`）。核对依据为 2026 A 题原文、仓库原版评估器、冻结 CSV、2026 官方格式与上传手册，以及 V1 审阅稿。V1 保持不变，V2 另存。此清单不属于参赛论文正文。

| 提示词条款 | 判定 | V2 动作或理由 |
|---|---|---|
| 总原则 1：不虚构数据、消融与显著性 | 采纳 | 只从冻结表读取数值；不新增实验或统计显著性主张。 |
| 总原则 2：正文不写成代码日志 | 采纳并限定 | 移除正文路径、哈希、内部 Gate/Stage/策略代号；保留必要的 B0/B1/B2A 方案标签及首次定义。复现信息集中附录。 |
| 总原则 3：每问说明本质、变量、约束、目标、算法、结果、失效 | 采纳 | 补短模型段与机制段；不为满足清单机械重复七个同义小节。 |
| 总原则 4：不添教材式背景 | 采纳 | 只解释影响本题官方时间线的 DAG、Pipe、DDR、L2 语义。 |
| 总原则 5：保留谨慎归因 | 采纳 | 保留候选频次非独立消融、命中不等于提速、100 图固定配置边界。 |
| 二：图题逐页核实，不加图内重复大标题 | 采纳 | 核对 10 幅正式图及正文引用；仅修正不能独立解释对象、指标、分母的图题。提示词写的页码只是定位线索，V1 实际为 40 页，不能照抄页码。 |
| 三：工程标签学术化 | 采纳并限定 | 正文首次定义结构化切图、场景 B 有界映射和 L2 感知排序；方案标签可保留在候选表/图中。`map_locality` 等内部名不作为方法标题。 |
| 四：附录应有三问核心代码节选（每段 40—80 行） | 不原样采纳 | 题目明确要求问题一、二的**可复现 Python 程序作为附件**，不是把不完整代码片段塞进 PDF。正文用短伪代码/步骤解释，附录给真实入口与依赖；完整源码在附件/仓库。问题三也列出真实源码。伪代码必须与程序一致。 |
| 五：每问七段固定结构 | 部分采纳 | 补齐“模型—求解—验证—解释”，但沿用现有可读章节，不机械增设重复小节。 |
| 六：问题一补决策、字典序目标与切图权衡 | 采纳 | 用 1—2 段说明算子归属、子图分核、核内序列、DAG 合法及官方 Makespan/新增搬运；解释完整层、弱连通分支、边界张量和 EFT 的不同作用。 |
| 六：代表案例结构成因、近线性退化 | 有条件采纳 | 可选已有实测案例说明现象；图结构原因若未做逐案证据分析，只写“提示/可能”，不冒充因果证明。回退是稳健选择规则，不是新调度算法。 |
| 七：问题二场景 B 机制与形式化 | 采纳 | 明确 M/V Pipe、输入按消费核去重、跨核张量去重、直接依赖逐边、同核驻留、共享 DDR、Spill；代理值仅筛选，官方 Makespan 唯一最终目标。 |
| 七：把 `map_locality` 写成有界算法 | 采纳但按源码校正 | 实际实现的局部性策略按预估完工、跨核载荷、传输数、重复输入、Pipe 负载的元组择核；迁核上限两步、搜索宽度 12；存储超额是代理的后置判据，不能声称精确模拟 Spill。 |
| 七：增独立问题二流程图 | 暂不采纳 | 现有总体流程图已覆盖“切图—映射—合法性—官方评测—回退”；新增图若只重复文字无新证据。用短算法步骤替代。 |
| 七：400 组、124 更快、9 同时少搬运、Spill 机理 | 采纳 | 按实际赢家严格配对；零最终退化注明由回退条件保证，不归因于单一策略。DDR 时序/关键路径解释写为可能机理而非已证明分解。 |
| 八：L2 官方语义与 2×2 设计 | 采纳 | 保留发起时查、完成时填、FIFO、独立 L2/DDR 带宽；写清“同方案硬件效应”“同硬件方案效应”及其不同分母。 |
| 八：L2 排序评分表达式 | 有条件采纳 | 源码使用两套加权分数加确定性字典序，**不是纯字典序**；只写与源码一致的候选优先级与官方回退，不虚构简单线性 Makespan 预测式。 |
| 八：独立问题三流程图 | 暂不采纳 | 总体流程图和 2×2 结果图已表达关键依赖；新增图须有不可替代的解释作用。 |
| 八：1.0248 与反例分析 | 采纳 | 保持“均值逐例比”口径；case_050/k4 与 case_067/k2 只用于说明命中不等于提速，不把单例当普遍规律。 |
| 九：讨论增递进、优势、局限、失效边界 | 采纳 | 以“结构划分→资源映射→存储层次感知”为主线；局限绑定受影响的主张；未来工作只保留最能解决现有不确定性的方向。 |
| 十：正文 25—30 页、附录压缩到代表案例/电子 CSV | 不按数字采纳 | 官方 2026 格式未规定这一页数比例。题目明确要求问题一、二逐例 Makespan 和新增搬运，问题三逐例无/有 L2 的 Makespan、新增搬运、命中率在**附录**列出；不能仅用可选电子附件替代。已有完整宽表优先保留，必要时删去非必需冗余表。 |
| 十一：建议完整目录 | 部分采纳 | 沿用 1—8 章，增加必要小标题；不为目录形式打断论证，也不在格式未规定时新增占页目录。 |
| 十二：自然中文、避免夸大 | 采纳 | 主张紧邻对照数据，方法句回答动机和作用；删“显著/普遍/最优”等无证据词。 |
| 十三：工程复现信息只入附录 | 部分采纳 | 正文允许必要的“官方评估器”与“冻结基线”概念，路径、脚本名、版本哈希只入复现说明。**AI 披露例外**：官方 2026 规定要求辅助数据分析在相关结果前导或后面标注工具元数据，辅助编程在相应程序文件头标注，不能只在附录声明。 |
| 十四：全文 12 项检查 | 采纳 | 图表/公式双向引用、三问五要素、附录要求、讨论、证据边界、术语与 AI 待核逐项查。AI 实际工具元数据未知，V2 仍仅为审阅稿，不伪填。 |
| 十五：另存 V2.docx 与修改说明 | 采纳 | 不覆盖 V1；修订前后页码以最终渲染 PDF 核实。无真实证据处在说明文件标注，论文不写虚构占位。 |
| 十六：优先级 | 调整后采纳 | P0 先补题目原文要求但 V1 缺失的问题三逐例搬运/命中率；P1 补问题二、三的模型与算法解释并去工程词；P2 改讨论、问题一和图题。原提示把 PDF 代码节选排在前列，与题目“源码附件”要求和用户核心目标不符。 |

## 统一论证句

本文把多核调度分为结构划分、共享资源下的核映射与存储层次感知调度；各阶段用可解释代理提出少量合法候选，最后由对应场景的官方模拟器按 Makespan 择优，并用回退和同案例配对限制错误归因。该句的适用范围仅为题目给定的 100 图、固定配置与官方实现。

## W1 主张—证据与论文落点

| 主张或必交结果 | 冻结证据字段／源码 | V2 落点 |
|---|---|---|
| 问题一结构切图、EFT 与回退的真实机制 | `solver_problem1.py` 的完整层窗口、弱连通分支、边界打包和 `ready_list_eft_schedule`；`candidate_manager_problem1.py` 的官方字典序选择 | 4.1—4.5 的动机、决策变量、合法性和求解步骤；图1总体关系、图2—4结果；不把候选入选频次写成独立消融。 |
| 问题一 100 例 1—5 核 Makespan、额外搬运 | `figures/q1/tables/appendix_problem1_wide.csv` 的 `makespan_k1`…`makespan_k5`、`added_copy_bytes_k1`…`added_copy_bytes_k5`；均值来自 `figures/q1/tables/` 冻结汇总 | 正文表2、附录 A1/A2；正文加速比先逐例求比再平均。 |
| 问题二场景 B 专用映射代理、最多两次局部迁核及官方回退 | `solver_problem2.py` 的 `_greedy_assignment`、`scene_b_mapping_proxy`、`_bounded_relocation_search`；`candidate_manager_problem2_final.py` 的最终合法性与官方择优 | 5.1—5.3：代理公式/有界步骤只作候选筛选，关键路径、Pipe、去重输入、跨核传输、共享 DDR 下界和存储超额各有定义；表4/5、图5—7呈现官方结果。 |
| 问题二 124/400 更快、9 组同时间少搬运、逐例必交值 | `figures/q2/tables/selected_results.csv` 的 `makespan_cycles`、`added_copy_bytes`、`spill_added_copy_bytes`、`stage1_makespan_cycles`、`stage1_added_copy_bytes` 等；`aggregate_by_core.csv` | 5.3 和表4/5严格配对；附录 B1/B2 逐例 Makespan/新增搬运，B3 的 Spill 保留为诊断。 |
| 问题三 L2 排序与共享输入迁核真实机制 | `solver_problem3.py` 的两套加权评分和外层确定性元组；`solver_problem3_mapping.py` 的共享输入单子图迁核；问题三原版评估器 | 6.1—6.3：同方案硬件比、同硬件方案比两条公式与反例；图8—10。不把代理评分称为官方事件仿真。 |
| 问题三四种方案×硬件组合逐例 Makespan | `figures/q3/tables/case_core_2x2.csv` 的 `baseline_no_l2_makespan`、`baseline_l2_makespan`、`final_no_l2_makespan`、`final_l2_makespan`；`curve_1_to_5.csv` | 正文表6、图8/9、附录 C1—C4。五核 1.0248 与 1.0121 均为先逐例求比后取均值。 |
| 问题三题目原文要求但 V1 缺失的逐例附加指标 | 同一 `case_core_2x2.csv` 的四个 `*_added_copy_bytes` 及两种有 L2 的 `*_cache_hit_rate_bytes`；无 L2 的命中率无定义 | **V2 必补附录 C5—C8 四种组合的额外搬运、C9—C10 两种有 L2 组合的字节命中率；无 L2 命中率标“不适用”。** 500 个 case-core 每表逐格列出。 |
| 复现与完整程序 | 问题一 `solver_problem1.py`/`candidate_manager_problem1.py`，问题二 `solver_problem2.py`/`candidate_manager_problem2_final.py`，问题三 `solver_problem3.py`/`solver_problem3_mapping.py`，相关运行入口及配置 | 附录 D 集中说明真实源码和冻结 CSV；题目规定的可复现 Python 程序另备附件，待队伍按上传手册核实并上传。正文只放短算法步骤，不用残缺代码片段替代附件。 |

图题、表题、公式位置在 DOCX 构建后逐一核对。问题二、三已有候选流程图与现有总体流程图高度重合，且问题三流程图含内部实验用语；V2 不因数量指标机械加入两图。官方要求的 1—5 核曲线继续用现有结果图。

## 必守边界

2026 官方上传手册允许非必要论文附件，大小不超过 50 MB，但题目原文要求问题一、二的可复现 Python 程序作为附件；完整代码包需要队伍按官方命名与上传时段另行核实。MD5 对应的最终 PDF 生成后不得再修改；本次 V2 审阅稿不取代最终提交版。

## 原提示第十四节的 12 项逐一验收

| 项 | 判定及实际核查 |
|---|---|
| 1 图题存在 | 采纳。图 1—10 均有独立图题，预览中位于图下。 |
| 2 编号连续 | 采纳。正文图 1—10、表 1—6 连续；附录依法采用 A1—A2、B1—B3、C1—C10 分组编号，不能强改为正文连续数码。公式编号延续 V1 并增至式（9）。 |
| 3 引用有实物 | 采纳。正文图表引用与图题、表题核对，公式说明紧邻式子；泛用格式检查器仅误判附录字母编号。 |
| 4 正文无 `.py` | 采纳。审计脚本检查参考文献前的正文，源码名集中附录 D。 |
| 5 正文无工程标签 | 采纳。`artifact`、`hash`、`full-root`、`Gate A`、`Stage 1`、`map_locality` 等内部名均不在正文。必要的方案简称 B0/B1/B2A 已解释。 |
| 6 三问五要素 | 采纳。问题分析、模型、求解、结果、解释分别落实于 4—6 章；每问同时写明适用边界。 |
| 7 三问核心代码 | **不按原形式采纳。** DOCX 未插入 40—80 行片段；用真实算法步骤和附录 D 源码索引解释核心逻辑。问题一、二的完整可运行 Python 程序属于题目要求的独立附件，仍待队伍打包核验，不能以片段冒充完成。 |
| 8 讨论四要素 | 采纳。7.1—7.3 包含递进关系、优势、限制、失效边界及可检验的改进方向。 |
| 9 新增结论有依据 | 采纳。逐例附录 15 表共 7,500 格与冻结 CSV 一致；新机制表述按实际程序/官方语义，未新增实验结果或统计显著性。 |
| 10 避免夸大 | 采纳。对机制使用“可能/提示/相容”，不称代理最优、普遍最优或统计显著。 |
| 11 AI 待核痕迹 | 采纳为**最终提交阻断项**。实际工具元数据未获队伍确认，V2 保留审阅标记而不伪填；最终 PDF 前必须补全并再次检查。 |
| 12 术语统一 | 采纳。正文统一使用“场景 A / 场景 B / L2”、Makespan、Spill、DDR；图题保留必要的方案简写。 |

原提示第十五节的 V2 DOCX 与 `修改说明.md` 均已单独生成；V1 未覆盖。第十五节“源码节选来自真代码”的前提只有在确实插入节选时才适用，本稿没有虚构节选。第十六节的优先级已按官方必交内容重排，先补 C5—C10，再加强两问建模和正文表达。
