# 2026 华为杯 A 题问题二：场景 B 多核切图与调度分析报告

> 本报告仅覆盖问题二。数值结论以仓库中的原版 `code/multicore_cut_evaluate_problem_2.py` 为唯一口径；估计模型只筛选候选，不替代官方模拟器。本文是建模与实现依据，提交前须由参赛队人工核对、改写。

## 1. 题目、版本与交付目标

### 1.1 输入与版本锚点

| 对象 | 路径或版本 | SHA-256 / 状态 |
|---|---|---|
| 原题 DOCX | `通用神经网络处理器下的多核调度问题.docx` | `d03f1d9a39f1fcae23467cc9fd384cbc61e4f13fc71f3567bdf44e1f3bab0b46` |
| 固定配置 | `data/config.txt` | `dcd10de54b23f8366428fb24e828812b1da9549e6eae4a3c3f38604fe5ae77b9` |
| 问题二官方评估器主文件 | `code/multicore_cut_evaluate_problem_2.py` | `0b39f84d5ec0a7fba9a4c92a598a9044b97ab79c71393824c1ba130ecfe6c464` |
| 当前 Git 版本 | `b7389703f8fd2f418dc2e60a81c18fbbe527fcec` | 建模终检前外部快进同步；问题二受保护源码未因此改变 |
| 交接基准 | `6755125513f8198d66a7352ef8cde7fd7bfe5126` | 当前分支包含该提交 |

原题要求：在场景 B 下为全部测试用例生成 1～5 核合法方案，正文给出 1～5 核平均加速比曲线，附录逐例报告官方 Makespan 与总额外数据搬运量，并提供可复现 Python 程序。

### 1.2 模型合同

| 项目 | 问题二合同 |
|---|---|
| 任务定义 | 对每个计算图及请求核数，联合确定切图、子图到核心的映射和每核子图顺序，使官方场景 B Makespan 最小，并在主目标相同时减少额外 DDR 搬运。 |
| 数据 | 100 个 `case_*.json` 计算图、固定 `config.txt`、问题一六类切图初始化、问题二官方评估器；所有输入只读。 |
| 结论类型 | 500 个 case-core 最终方案及官方指标；每核数逐例加速比、均值、中位数、尾部分位数、失败率、搬运、Spill、跨核有效载荷、传输数和利用率。 |
| 主模型 | 有限候选集上的场景 B 模拟器反馈优化：多切图初始化 + 通信/关键路径感知核映射 + 低核赢家继承 + 官方全局回退。 |
| 数学定义 | 决策包含算子到子图划分、子图到核心映射及每核拓扑合法顺序；使用本方案冻结的字典序选择规则 `(Makespan, added-copy bytes)`。 |
| 实现 | Python 确定性候选生成、内容寻址缓存、官方合法性检查、官方模拟器评分、断点续跑和完整失败记录。 |
| 验证 | 12 例开发、18 例验证、70 例最终报告分层；相同 case-core/切图严格配对；最终 100 例均生成结果。开发或验证门槛只决定增强候选是否进入最终组合，不阻断基础组合的全量输出。 |
| 风险与回退 | 任一增强失败、非法或无收益时回退到已验证的阶段一实际赢家；不得让局部搜索覆盖安全方案，不修改官方评估器。 |

## 2. 场景 B 语义与约束

将原计算图记为有向无环图 \(G=(V,E)\)，经切图得到子图 DAG \(H=(S,A)\)。请求核心数为 \(k\in\{1,2,3,4,5\}\)。

决策变量包括：

- \(z_{v,s}\in\{0,1\}\)：非 COPY 算子 \(v\) 是否属于子图 \(s\)；
- \(x_{s,c}\in\{0,1\}\)：子图 \(s\) 是否映射到核心 \(c\)；
- \(\pi_c\)：核心 \(c\) 上子图的执行序列。

必须满足：

1. 每个非 COPY 算子恰好属于一个子图，每个子图恰好映射到一个核心；
2. 子图收缩图保持 DAG；
3. 每个 `sgid` 在全部 `core_schedules` 中恰好出现一次；
4. 每核顺序不违反子图依赖；
5. 方案只能包含官方接口允许的 `node_to_subgraph` 和 `core_schedules`；
6. L1/UB 容量、Pipe 互斥、共享 DDR 排队、跨核 500 cycles 同步和 Spill 均由官方代码执行。

### 2.1 场景 B 的任务、通信与存储状态转移

场景 B 对一个方案的执行语义如下，这些语义同时构成模型约束而非仅是软件实现细节：

1. 每个请求核心都对应一个 Task；该核心上的全部子图合并进入此 Task。空核心对应零操作空 Task，在执行效果上等价于没有工作。同一核心的子图按 \(\pi_c\) 顺序约束执行，但不同 Pipe 可按官方核内调度规则并行。
2. 同核相邻或非相邻子图之间若存在张量依赖，张量可继续驻留在该核心私有 L1/UB 中，边界不插入 COPY。驻留张量持续占用容量，直到官方生命周期分析判定最后一次使用结束。
3. 若张量依赖跨核心，源 Task 插入 COPY_OUT，目标 Task 插入 COPY_IN。目标 COPY_IN 只有在源 COPY_OUT 完成并经过固定同步延迟 \(d_{\mathrm{sync}}=500\) cycles 后才可就绪；该延迟只作用于实际依赖链，不能按“传输总数乘500”估计 Makespan。
4. 跨核普通张量按 \((t,c_{\mathrm{src}},c_{\mathrm{dst}})\) 去重；官方识别的直接 Op–Op 跨核依赖按边分别插入通信。COPY_OUT 与 COPY_IN 两端都会形成实际搬运。
5. 全部访问 DDR 的图输入读取、图输出写回、跨核 COPY 和 Spill COPY 竞争同一全局 DDR，带宽为 \(r_{\mathrm{DDR}}=60\) bytes/cycle。官方离散事件模拟器按同时活跃搬运公平共享带宽，因此简单相加各搬运时长不能重建 Makespan。
6. 当固定核内顺序导致 L1/UB 容量不足时，官方第二阶段按未来使用位置自动插入换出和换入；Spill 同时改变搬运、DDR 排队和后续就绪时间。零 Spill 不是最终目标。
7. 每个算子还必须满足数据依赖、Task 激活时间、对应 Pipe 空闲和所需缓存空间同时可用；官方事件循环推进到下一个完成、激活或空间释放事件。

对张量 \(t\)，记产生和消费它的核心集合分别为 \(P_t\) 和 \(C_t\)，图输入张量集合为 \(I\)，张量字节数为 \(b_t\)。筛选模型中的通信口径为：

\[
B_{\mathrm{in}}=\sum_{t\in I}|C_t|\,b_t,
\]

\[
B_{\mathrm{cross}}=\sum_t b_t\left|\{(p,c):p\in P_t,c\in C_t,p\ne c\}\right|+B_{\mathrm{direct}},
\]

其中 \(B_{\mathrm{direct}}\) 是官方逐边插入的直接跨核依赖有效载荷。第一式按消费核心对输入去重，第二式按 `(tensor, source_core, target_core)` 去重。输出写回、Spill、缓存峰值和实际共享 DDR 竞争最终均以官方模拟器为准。

## 3. 目标函数与评价口径

本方案冻结如下字典序选择规则：

\[
\min \; F(P)=\bigl(T_{\mathrm{official}}(P),\ B_{\mathrm{add,official}}(P)\bigr).
\]

Makespan 是绝对主目标；只有 Makespan 相同才比较新增搬运。Spill、跨核有效载荷和传输数是解释变量与候选筛选信号，不替代主目标。

对 case \(i\) 和核数 \(k\)，加速比定义为

\[
S_{i,k}=\frac{T_{i,1}^{\mathrm{single}}}{T_{i,k}},
\]

其中分母必须是相同 case 的最终官方 Makespan，分子严格采用题目规定的整图单核 `single`，不能换成单核切图诊断候选。平均加速比采用逐例比值的算术平均，不使用均值之比。

## 4. 主求解模型：有限候选集上的模拟器反馈调度

### 4.1 候选集合

对每个 case-core，建立以下候选池：

1. 安全基线：阶段一 `selected_results.csv` 的实际最终赢家，可能是 `single`、五种原生切图之一或低核继承；
2. 字面 `single`：每个请求核数均显式引用阶段一中整图单任务、其余核为空的官方结果；
3. 五个固定切图的原映射：`B0、B1、B2A-w4、B2A-w8、B2A-w16`，从阶段一经哈希复核的官方结果直接引用；
4. 固定切图、场景 B 专用映射：对上述五个切图分别生成唯一冻结策略 `map_locality`；
5. 递推继承：将上一核数的最终赢家通过追加空 `core_schedules` 继承到当前核数；
6. 原方案始终保留，任何增强候选不能替代或删除全局回退。

因此每个 2–5 核 cell 显式记录 12 个逻辑候选（题目要求的 `single`、五个原映射、五个 `map_locality`，以及额外的阶段一实际赢家全局回退）；3–5 核再加入一个递推继承候选。阶段一实际赢家与 `single` 或某个原映射相同时允许内容去重，但各候选身份和来源记录均保留。

递推继承只需保留上一核数最终赢家。因为追加空核不改变活跃任务，若第 \(k-1\) 核最终组合已支配所有更低核数，则其继承方案也支配从更低核数重复继承的方案。尽管如此，每个目标核数仍运行官方合法性检查和官方评估，不把这一结构性质直接当作分数。

### 4.2 `map_locality` 筛选模型

固定切图后，先按稳定拓扑序构造映射，再进行至多两次单子图迁核。估计完工时间为

\[
\widehat T=\max\{T_{\mathrm{dep}},T_{\mathrm{pipe}},T_{\mathrm{DDR}}\},
\]

其中 \(T_{\mathrm{dep}}\) 是按稳定拓扑序正向递推得到的依赖时刻代理，单位为 cycles；\(T_{\mathrm{pipe}}\) 是各核 Cube/Vector Pipe 累计负载代理，单位为 cycles；\(B_{\mathrm{scheduled}}\) 是代理模型统计的图输入、图输出与双端跨核搬运总量，单位为 bytes；\(T_{\mathrm{DDR}}=\lceil B_{\mathrm{scheduled}}/r_{\mathrm{DDR}}\rceil\) 是共享 DDR 工作量代理，单位为 cycles。上述量均不保证构成官方 Makespan 的上界或下界。`map_locality` 以

\[
(\widehat T,B_{\mathrm{cross}},N_{\mathrm{cross}},B_{\mathrm{scheduled}},M_{\mathrm{excess}})
\]

作确定性排序，其中 \(N_{\mathrm{cross}}\) 是跨核传输数，单位为次，\(M_{\mathrm{excess}}\) 是代理生命周期下超过 L1/UB 容量的最大字节数。核心编号和子图编号作为最终平局规则。该代理不声称预测官方 Makespan，只负责从巨大离散空间中形成小候选集。

### 4.3 官方闭环与确定性

每个候选均执行：计划字段校验 → 子图 DAG 和核内顺序校验 → 规范 plan 哈希和去重 → 内容寻址缓存查询 → 原版官方问题二评估 → 指标压缩和完整哈希记录。最终按本方案冻结的字典序选择规则择优。随机算法不是主模型，全部规则确定性运行。

## 5. 可选增强：受限切图边界修补

截止时间内，切图修补不得阻塞主模型全量运行。只有主组合已形成可提交的 500 行结果且仍有预算时，才在独立新模块中尝试：

- 只选择当前组合中高 Spill、高跨核关键边或严重负载不平衡的 case-core；
- 必须改变 `node_to_subgraph`，并通过规范 plan 签名证明与已有映射搜索不等价；
- 每个种子最多 1～2 次边界算子迁移、合并或拆分，最多两个新方案；
- 始终保留当前最终赢家；开发/验证失败只停止该增强，不停止问题二全量结果。

该增强属于同一“受限邻域 + 官方反馈”模型族，不把模拟退火、禁忌搜索或贪心本身表述为创新。

## 6. 实验设计与停止规则

### 6.1 已有证据

- 阶段一：100 case × 1～5 核基线完整，失败率为 0；
- 阶段二开发：12 例 `map_locality` 共 240/240 组合法、0 失败；主要36个未见 case-core 中8个降低 Makespan，总节省125,470 cycles，平均改善0.5365%，但收益集中，必须保留回退；
- 阶段三探针：3个固定映射 case-core 的最终组合改善为0，因此停止当前两种核内排序候选的扩展。该结论不等价于证明所有可能的核内排序均无效。

### 6.2 冻结的数据分层与身份

问题二采用以下固定分层；它与问题三文件相互独立，问题二实现中将另存为 `problem2_preregistered_split.json`，不得修改问题三划分文件。

- 开发12例：`001, 016, 024, 044, 047, 048, 050, 051, 058, 064, 067, 100`；
- 验证18例：`010, 017, 018, 019, 026, 027, 031, 032, 042, 046, 059, 060, 062, 065, 082, 085, 089, 092`；
- 最终报告70例：`002–009, 011–015, 020–023, 025, 028–030, 033–041, 043, 045, 049, 052–057, 061, 063, 066, 068–081, 083–084, 086–088, 090–091, 093–099`。

以上三个完整 case 列表按键排序后规范 JSON 的 SHA-256 为 `09f131d078750d9bf8dd5db71835850195d9230924406918e93204c1daa7daaa`。划分沿用种子 `20260925`，在读取问题二 Stage 4 或问题三优化收益前已冻结。

主组合“阶段一实际赢家 + 五类 `map_locality` + 递推继承”参数已经由开发集冻结，18例和70例上不再改规则。18例的技术验收要求为72/72 case-core 完成、所有新候选合法、零失败；科学结果无论正负都如实报告。由于原题要求全部100例，且每格有官方评分后的阶段一回退，科学收益不足不会删除主组合，也不会造成70例缺失。

Stage 4 是唯一受门禁控制的可选增强：开发集须相对主组合在至少3个不同 case、至少6/48个 case-core 严格降低 Makespan；冻结后，验证集须在至少4个不同 case、至少8/72个 case-core 严格降低 Makespan，且零失败。门槛仅是预注册的资源决策规则，不是统计显著性声明。任一门槛失败，Stage 4 不进入70例，但主组合仍完成全部100例。

### 6.3 截止前主线

1. 冻结 `map_locality` 参数 `max_moves=2, search_width=12`；
2. 先完成最终候选组合的最小真实 smoke 和独立 P1；
3. 使用独立输出与缓存完成全部100例、1～5核；
4. 若映射或继承候选失败，逐格回退阶段一赢家并记录失败，不允许整批缺行；
5. Stage 4 边界修补是可选增强，失败时仍以冻结主组合生成500行正式结果。

上述资源决策门槛只决定是否扩大 Stage 4，不是统计显著性结论。统计报告采用严格配对的效应量、改善 case 分布、符号检验或 bootstrap 区间，并明确零差值和全局回退造成的混合分布。

## 7. 输出与论文证据

正式运行至少产生：

- 500 行最终 case-core 官方结果；
- 500 行逐例加速比；
- 1～5核平均、Median、P05/P95、最差 case 和失败率；
- 每格 Makespan、added-copy、Spill、跨核有效载荷、传输数、每核利用率；
- 全部候选、去重、失败、求解时间、官方评估时间、缓存命中和身份哈希；
- 1～5核平均加速比折线图和逐例附录表。

论文创新点应限定为：面向官方场景 B 语义的“多切图候选—关键路径/通信感知映射—有效核数递推继承—官方模拟器反馈全局回退”闭环，以及对共享 DDR、同步等待、核内驻留、Spill 与通信的联合消融。不能把常规贪心或局部搜索单独称为创新。

## 8. 假设、风险与复验

### 8.1 关键假设

1. **关键**：仓库原版场景 B 评估器是唯一数值口径。若代码与题意文字冲突，以代码为准并报告差异；不得修改评估器。
2. **关键**：阶段一基线方案、图、配置和官方结果在复用前逐项通过内容哈希验证。失配时缓存失效并在新目录重跑。
3. **关键**：请求核数允许存在空核；继承方案仍必须由官方接口和合法性检查接受。

### 8.2 可放宽假设

1. `map_locality` 的代理排序只近似官方时间；通过保留全部官方回退并对短名单正式评估放宽。
2. 当前不继续核内排序是截止时间下的资源决策；论文把它报告为已测候选的负结果，不作普遍无效结论。
3. Stage 4 不进入关键路径；若时间充足，可在主结果冻结后以独立版本和独立消融追加。

主要风险包括官方评估超时、缓存跨环境失效、收益集中、增强候选非法和磁盘不足。对应措施是严格缓存键、失败隔离、逐格回退、断点续跑、独立输出目录和每批运行前检查磁盘。

## 9. 理论依据

- Kelley, J. E., and Walker, M. R. (1959). “Critical-Path Planning and Scheduling.” *Proceedings of the Eastern Joint Computer Conference*, 160–173. DOI: `10.1145/1460299.1460318`。该文用于关键路径与最早完工分析的基础。
- Sinnen, O., and Sousa, L. A. (2004). “List scheduling: extension for contention awareness and evaluation of node priorities for heterogeneous cluster architectures.” *Parallel Computing*, 30(1), 81–101. DOI: `10.1016/j.parco.2003.09.002`。
- Wang, Y., Li, K., Chen, H., He, L., and Li, K. (2014). “Energy-Aware Data Allocation and Task Scheduling on Heterogeneous Multiprocessor Systems With Time Constraints.” *IEEE Transactions on Emerging Topics in Computing*, 2(2), 134–148. DOI: `10.1109/TETC.2014.2300632`。该文支持任务映射与本地数据配置需联合考虑；本文不沿用其能耗目标。
- Huang, J. Y., Li, R., Jiao, X., Jiang, Y., and Chang, W. (2020). “Dynamic DAG Scheduling on Multiprocessor Systems: Reliability, Energy, and Makespan.” *IEEE TCAD*, 39(11), 3336–3347. DOI: `10.1109/TCAD.2020.3013045`。该文用于说明 DAG 映射、动态完工时间与 Makespan 的联系；本文的最终口径仍为赛题官方模拟器。

上述文献由 OpenAlex 与 AnySearch 双引擎交叉检索得到；论文提交前仍应由队伍打开 DOI 出版页面复核格式。
