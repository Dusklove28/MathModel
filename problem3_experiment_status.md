# 问题三实验状态（2026-09-25：最终留出集与消融已完成）

本文件只记录已经运行并核验的结果。问题三的计算实验已完成；论文图表、复现清单及独立 P2 编程终检尚未完成，不把实验完成等同于论文交付完成。运行命令见 `problem3_linux_run.txt`。

**本机路径提示**：Linux 原产物位于 `artifacts/problem3_*`。本机在 `artifacts/` 中解压的归档自带 `artifacts/` 前缀，因此新取回的结果位于 `artifacts/artifacts/problem3_*`。下文新增的最终结果路径均按本机解压位置填写；不要将其与旧的 `artifacts/problem3_*` 混用。

## 资产与冻结范围

- 新归档的 G0 审计 `artifacts/artifacts/problem3_g0/audit.json`：`passed=true`，`missing=[]`，`mismatches=[]`。问题二 Stage1 的 500 组 manifest、赢家方案和官方结果均存在，图、方案、结果文件哈希逐项匹配。配置与问题二官方依赖的 LF 规范化哈希与历史 Linux 记录一致；历史和本次 ZIP 运行的 `git_head` 为空，历史问题二求解器/运行器哈希与当前代码不同，因此仅复用经验证的方案和官方结果，不声称问题二全量冷启动独立复现。问题三的完整官方依赖与实现哈希见各运行目录 `run_identity.json` 及最终报告 `summary.json`。
- 配置 LF SHA256：`dcd10de54b23f8366428fb24e828812b1da9549e6eae4a3c3f38604fe5ae77b9`；问题三官方评估器 LF SHA256：`eab1504dead881f4b67c0f0498cbc2dbbd9039dc3c9d198c6af58773c127eeb0`。L2 为 1 MiB / 250 B·cycle⁻¹，DDR 为 60 B·cycle⁻¹，跨核同步 500 cycles。
- 12 个开发例、18 个盲化验证例和 70 个留出例由 `problem3_preregistered_split.json` 固定，种子为 20260925；划分先于问题三收益检查。当前 checkout 使用 `python -m unittest discover -s tests -p 'test_problem3*.py' -v`（4 项）和 `python -m unittest discover -s tests -p 'test_solver_problem3.py' -v`（3 项）复核，均退出码 0。

## G1：相同方案的官方配对评估

100 例 × 1–5 核共 500/500 组成功，问题三官方评估 500 次；问题二已有结果逐格核验复用，另在 3 例 × 2–5 核做了 12 次官方复评。`artifacts/problem3_report_g1_v4/case_core_2x2.csv` 有 500 行数据，`curve_1_to_5.csv` 有 5 行曲线数据。此阶段两行方案相同，故 2×2 表只完整表达硬件加入 L2 的配对效应。

下面的加速比为每例 `T_noL2/T_L2` 的算术均值，非均值之比；胜出数为 100 例中严格大于 1 的例数。置信区间为按 case 重采样的均值百分位区间。

| 核数 | 平均加速比 | 中位数 | 胜出例数 | 95% 区间 |
|---:|---:|---:|---:|---:|
| 1 | 1.008622 | 1.000000 | 25 | [1.001608, 1.018939] |
| 2 | 1.002941 | 1.000006 | 51 | [1.001352, 1.004880] |
| 3 | 1.009620 | 1.000054 | 55 | [1.005347, 1.014487] |
| 4 | 1.018043 | 1.001145 | 64 | [1.011253, 1.026155] |
| 5 | 1.025657 | 1.001406 | 63 | [1.016597, 1.036097] |

并行加速比必须注明单核分母。问题二 Stage1 **请求 k=1 时的冻结赢家**作为分母，5 核平均逐例加速比为 **3.208500**；在同一批 Stage1 **请求 k=1 的所有已评估合法候选**中逐例选最快官方 makespan 作分母，则为 **3.101072**，有 75 例严格快于前一种单核分母。后一口径的入选候选方案和官方结果均经文件 SHA256 与规范 JSON 哈希核验。报告同时保留两列；跨请求核数选择“只有 1 个活跃核”的计划属于第三种定义，不能与 3.101 混写。

官方时间线例证：`case_050/k4` 从 46748 降为 44398 cycles，220 次 L2 命中且无淘汰；`case_001/k2` 两种硬件均为 116868 cycles，0 次命中；`case_067/k2` 从 31892152 增至 32130317 cycles，虽有 676 次命中，仍出现 1916 次 FIFO 淘汰。上述现象说明命中率只是解释变量；该反例的时间线与缓存读、DDR 竞争及淘汰共同作用相符，不能仅凭命中率推断 makespan。证据在 `artifacts/problem3_timeline_evidence/`。

## G2/G3：预注册优化与最终留出集

两条分支均保留上一阶段方案作为回退，按官方问题三 makespan 最小择优。排序分支固定原切图和核心映射；映射分支在排序赢家上至多生成两个共享输入的单子图迁移候选。所有已评估候选保持官方合法性，失败组数为 0。下表的严格改善：排序分支相对 G1，映射分支相对已选排序方案；官方调用数为**本阶段调用数**，不累计前阶段。

| 分支 | 阶段 | 成功组数 | 严格改善组 | 覆盖例数 | 本阶段官方调用 | 失败组 |
|---|---|---:|---:|---:|---:|---:|
| 固定映射排序 | smoke | 12/12 | 4 | 2 | 15 | 0 |
| 固定映射排序 | 开发 | 48/48 | 9 | 6 | 40 | 0 |
| 固定映射排序 | 验证 | 72/72 | 23 | 14 | 84 | 0 |
| 固定映射排序 | 留出 | 280/280 | 85 | 46 | 385 | 0 |
| 共享输入映射 | smoke | 12/12 | 3 | 2 | 11 | 0 |
| 共享输入映射 | 开发 | 48/48 | 3 | 2 | 18 | 0 |
| 共享输入映射 | 验证 | 72/72 | 24 | 11 | 61 | 0 |
| 共享输入映射 | 留出 | 280/280 | 65 | 40 | 242 | 0 |

smoke 例与开发例重叠；每条分支的唯一 `case-core` 组数均为 400（100 例 × 2–5 核），不能把上表组数简单相加。排序分支累计 524 次问题三官方候选评估，映射分支累计 332 次。留出阶段分别耗时约 1790.7 s、2383.9 s。逐阶段记录在 `artifacts/artifacts/problem3_g2_ordering_v2/invocations/` 和 `artifacts/artifacts/problem3_final_mapping_v2/invocations/`；两个分支有独立身份哈希、缓存命名空间、逐组方案/官方结果哈希与严格断点校验。验证阶段达到预定的至少 2 例改善门禁，随后按冻结规则运行留出集。

## 最终 2×2 消融与 1–5 核曲线

- 最终权威表 `artifacts/artifacts/problem3_report_final/case_core_2x2.csv`：500 行数据、78 列，100 例 × 1–5 核无缺格；500 组原方案和最终方案均合法。四个 makespan 构成“冻结问题二原方案 / 问题三最终方案”×“官方问题二无 L2 / 官方问题三有 L2”的同组配对结果。
- 曲线 `artifacts/artifacts/problem3_report_final/curve_1_to_5.csv`：5 行数据。详细口径、按 case bootstrap 区间、输入/代码身份哈希见 `artifacts/artifacts/problem3_report_final/summary.json`。208 组最终方案与 G1 不同，报告为这些方案额外调用官方问题二评估器 208 次，补齐无 L2 列。
- 208 组方案改变，其中 180 组在有 L2 的官方 makespan 上严格改善，覆盖 80/100 例；0 组退化（每组保留回退方案）。70 例留出集的 280 组中，133 组严格改善，覆盖 58/70 例；0 组退化。

下表“硬件收益”是同方案 `T_noL2/T_L2` 的逐例均值；“调度收益”是 `T_原方案,L2/T_最终方案,L2` 的逐例均值。均为**比值的均值**，不是均值之比；胜出例数和 95% 区间针对 L2 下调度收益，区间按 case bootstrap。

| 核数 | 原方案硬件收益 | 最终方案硬件收益 | L2 下调度收益 | 无 L2 下调度收益 | L2 下严格胜出例数 | 调度收益 95% 区间 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 1.008622 | 1.008622 | 1.000000 | 1.000000 | 0 | [1.000000, 1.000000] |
| 2 | 1.002941 | 1.003483 | 1.010963 | 1.010408 | 43 | [1.006298, 1.016749] |
| 3 | 1.009620 | 1.008184 | 1.014292 | 1.015767 | 51 | [1.008551, 1.020877] |
| 4 | 1.018043 | 1.017181 | 1.007234 | 1.008173 | 41 | [1.004070, 1.011232] |
| 5 | 1.025657 | 1.024819 | 1.012104 | 1.012962 | 45 | [1.006896, 1.017945] |

5 核有 L2 的逐例平均并行加速比，若分母为同例冻结 k=1 的 L2 makespan，原方案为 **3.261570**，最终方案为 **3.300881**。70 例留出集按 2/3/4/5 核分开的调度收益均值依次为 **1.011614 / 1.015699 / 1.009238 / 1.012742**，严格改善例数为 **29 / 37 / 32 / 35**，各核最差值均为 1.0。无 L2 下的方案改进幅度与有 L2 下接近，因此不能把全部调度收益归因于缓存感知机制；硬件收益、调度收益和交互项须分别解释。

## 论文与复现收尾（尚未完成）

1. 用最终表、曲线与官方时间线准备图表、表注和典型案例。G1 的正例、无收益例与反例已有时间线；若要论证最终排序/映射改变的机制，还需查看相应最终方案的官方时间线。
2. 生成 `results/复现清单.json`、三类数据/过程/结果图与总体/问题三流程图，并执行图表审计和独立 P2 编程终检；目前不能标记这些门禁为 `PASS`。
3. 等问题二方案定稿后统一论文口径、术语、单核分母和跨问题图表编号。问题二及整题论文未定稿，不在本文件中宣称全题完成。

通用缓存感知调度不作为原创性主张；本题可讨论的具体贡献是围绕“发起时查询、完成时填充、FIFO 字节容量、双带宽、跨核同步与核内存储”所做的受限候选构造和官方配对验证。
