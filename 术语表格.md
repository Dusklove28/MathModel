# 2026 华为杯 A 题问题二术语与符号表

> 只登记跨公式或章节复用的符号。临时求和下标在公式附近定义。

| 中文术语 | 英文术语 | 符号 | 定义 | 单位 | 首次出现 | 禁止混用 |
|---|---|---:|---|---|---|---|
| 原计算图 | original computation graph | \(G=(V,E)\) | 题目给定的 Op–Tensor 有向无环图 | 无量纲 | 分析报告第2节 | 不与子图 DAG 混用 |
| 子图 DAG | subgraph DAG | \(H=(S,A)\) | 切图收缩后以子图为节点的有向无环图 | 无量纲 | 分析报告第2节 | 不称“Task 图”替代其结构含义 |
| 子图 | subgraph | \(s\in S\) | 被分配到单一核心并按核内序列执行的一组算子 | 无量纲 | 分析报告第2节 | 不与单个算子混用 |
| 核心 | NPU core | \(c\in\{0,\ldots,k-1\}\) | 执行子图的 NPU 核心 | 无量纲 | 分析报告第2节 | 不把请求核数与实际活跃核数混写 |
| 请求核数 | requested core count | \(k\) | 方案 `core_schedules` 的长度，取1～5 | 个 | 分析报告第2节 | 不写成活跃核数 |
| 活跃核数 | used core count | \(k_{\mathrm{use}}\) | 至少执行一个子图的核心数 | 个 | 结果分析 | 不等同于 \(k\) |
| 切图变量 | partition variable | \(z_{v,s}\) | 算子 \(v\) 是否属于子图 \(s\) 的0–1变量 | 无量纲 | 分析报告第2节 | 不与核映射变量混用 |
| 核映射变量 | core-assignment variable | \(x_{s,c}\) | 子图 \(s\) 是否分配到核心 \(c\) 的0–1变量 | 无量纲 | 分析报告第2节 | 不与切图变量混用 |
| 核内序列 | intra-core order | \(\pi_c\) | 核心 \(c\) 上拓扑合法的子图执行序列 | 无量纲 | 分析报告第2节 | 不称全局拓扑序 |
| 官方执行时间 | official makespan | \(T_{i,k}\) | 官方场景 B 模拟器返回的 case \(i\)、核数 \(k\) 完工时间 | cycles | 分析报告第3节 | 不用代理 Makespan 替代 |
| 估计执行时间 | proxy makespan | \(\widehat T\) | 仅用于筛选映射候选的确定性代理；不保证是官方 Makespan 的上界或下界 | cycles | 分析报告第4节 | 不称官方分数 |
| 加速比 | speedup | \(S_{i,k}\) | \(T_{i,1}^{\mathrm{single}}/T_{i,k}\) | 无量纲 | 分析报告第3节 | 不用均值之比替代平均逐例比值 |
| 张量字节数 | tensor size | \(b_t\) | 张量 \(t\) 的数据大小 | bytes | 分析报告第2节 | 不与搬运次数混用 |
| 图输入集合 | graph-input tensor set | \(I\) | 原图逻辑位置为 DDR 且被图内算子读取的输入张量集合 | 无量纲 | 分析报告第2节 | 不与全部 DDR 张量混用 |
| 产生核心集合 | producer-core set | \(P_t\) | 产生张量 \(t\) 的子图所在核心集合 | 无量纲 | 分析报告第2节 | 不与消费核心集合混用 |
| 消费核心集合 | consumer-core set | \(C_t\) | 消费张量 \(t\) 的子图所在核心集合 | 无量纲 | 分析报告第2节 | 不与产生核心集合混用 |
| 输入读取量 | graph-input read bytes | \(B_{\mathrm{in}}\) | 图输入按消费核心去重后的读入字节量 | bytes | 分析报告第2节 | 不等同于跨核有效载荷 |
| 跨核有效载荷 | cross-core payload | \(B_{\mathrm{cross}}\) | 跨核张量三元组去重和直接跨核边产生的有效字节量 | bytes | 分析报告第2节 | 不等同于双向 DDR 搬运量 |
| 直接跨核边有效载荷 | direct cross-edge payload | \(B_{\mathrm{direct}}\) | 官方按直接 Op–Op 跨核依赖逐边插入的有效载荷 | bytes | 分析报告第2节 | 不参与普通张量三元组去重 |
| 计划搬运代理量 | scheduled-copy proxy | \(B_{\mathrm{scheduled}}\) | 代理模型统计的图输入、图输出与跨核双端搬运总量 | bytes | 分析报告第4节 | 不称官方新增搬运 |
| 总新增搬运 | added copy bytes | \(B_{\mathrm{add}}\) | 官方计划搬运量减原图固有搬运量 | bytes | 分析报告第3节 | 不与总计划搬运量混用 |
| 溢出搬运 | spill copy bytes | \(B_{\mathrm{spill}}\) | L1/UB 容量不足引入的换出和换入搬运 | bytes | 结果分析 | 不把零 Spill 当最终目标 |
| 跨核同步延迟 | cross-core synchronization delay | \(d_{\mathrm{sync}}\) | 源端写回完成后目标端读取需等待的固定延迟，配置为500 | cycles | 场景 B 约束 | 不乘以全部传输数估计 Makespan |
| DDR带宽 | shared DDR bandwidth | \(r_{\mathrm{DDR}}\) | 全部核心共享的 DDR 服务速率，配置为60 | bytes/cycle | 场景 B 约束 | 不视为每核独享带宽 |
| 依赖递推时刻 | dependency finish proxy | \(T_{\mathrm{dep}}\) | 代理模型沿子图 DAG 递推得到的依赖完成时刻 | cycles | 分析报告第4节 | 不称官方 Makespan |
| Pipe负载代理 | pipe-load proxy | \(T_{\mathrm{pipe}}\) | 各核心 Cube/Vector Pipe 累计负载的最大值 | cycles | 分析报告第4节 | 不包含全部等待，也不声明理论界 |
| DDR工作量代理 | DDR-work proxy | \(T_{\mathrm{DDR}}\) | \(\lceil B_{\mathrm{scheduled}}/r_{\mathrm{DDR}}\rceil\) | cycles | 分析报告第4节 | 不等同于事件模拟时间，也不声明理论界 |
| 跨核传输数 | cross-core transfer count | \(N_{\mathrm{cross}}\) | 普通张量三元组去重传输与直接边传输的总次数 | 次 | 分析报告第4节 | 不与跨核字节数混用 |
| 最大容量超额 | maximum memory excess | \(M_{\mathrm{excess}}\) | 代理生命周期下超过任一核心 L1/UB 容量的最大字节数 | bytes | 分析报告第4节 | 不等同于官方 Spill |
| 核内缓存峰值 | local-memory peak | \(M_{c,p}^{\max}\) | 核心 \(c\) 在存储域 \(p\in\{\mathrm{L1},\mathrm{UB}\}\) 的峰值占用 | bytes | 候选诊断 | 不与缓存容量混用 |
| 本方案字典序选择规则 | frozen lexicographic selection rule | \(F(P)\) | \((T_{\mathrm{official}}(P),B_{\mathrm{add,official}}(P))\) | cycles, bytes | 分析报告第3节 | 不称题目明定的官方评分函数；不进行线性加权 |
| 全局回退 | global fallback | — | 当前 case-core 的阶段一实际官方赢家 | — | 分析报告第4节 | 不等同于某一固定切图 |
| 空核继承 | empty-core inheritance | — | 在较低核方案后追加空核心序列形成高核请求方案 | — | 分析报告第4节 | 不称重新切图 |
| 内容寻址缓存 | content-addressed cache | — | 由图、配置、方案、评估器和实现哈希共同绑定的缓存 | — | 实现说明 | 不按绝对路径直接复用 |
