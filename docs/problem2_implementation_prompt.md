# 问题二实现提示词（复制给另一窗口）

你是多核计算图调度与数学建模专家。请在当前 Git 仓库中实现问题二的高质量求解方案；先阅读 `data/config.txt`、`README.md`、`solver_problem1.py`、`candidate_manager_problem1.py`、`code/multicore_cut_evaluate_problem_2.py`、`handoff/problem2_p20/P2-0_交叉评估分析.md`、`handoff/problem2_p20/p20_results.csv` 和 `handoff/problem2_p20/README.md`。若另一台设备另有题目原题，也请核对；本交接不依赖未入库的 PDF。以仓库内官方场景 B 评估器为唯一指标口径，不修改官方评估器，也不要把场景 A 的结论直接代入场景 B。未经我另行授权，不执行 Git 提交或推送；保留已有修改。

背景：问题一六方案（`single`、`B0`、`B1`、`B2A-w4/w8/w16`）在 `case_001/050/100`、2/4/5 核的场景 B 交叉评估共 54 项，全部成功。`case_001` 最优 B0、`case_050` 最优 B2A-w16、`case_100` 最优 B2A-w4。结果是三例探针，不代表 100 例总体。输入在多核重复读入可能产生新增 DDR 搬运，即使跨核有效载荷为零；Spill 和跨核传输可同时发生。固定同步延迟只影响实际关键路径，不可用 `500 × 全部跨核传输次数` 当 makespan 估计。

请按以下阶段实现，每阶段给出可复现结果和是否继续的判据：

1. **保全基线。** 先复核 P2-0 快照的定义、哈希和口径，并在新的输出目录做必要的 smoke test。`run_problem2_p20.py` 可从 `handoff/problem2_p20/frozen_groups/` 读取九组冻结签名。历史 `plans/records/` 是原机器快照，含当时的绝对路径及源码哈希，不要当作当前 checkout 的直接缓存。建立 1–5 核、100 个 case 的场景 B 基线，并保留原六方案及低核空核继承作为候选；记录官方 makespan、合法率、每核利用率、跨核有效载荷与传输数、总新增搬运、Spill、运行耗时，比较均值、中位数、最差分位数和失败率。控制实验预算，避免只用少数样例调参。
2. **场景 B 专用核映射。** 复用问题一图分析及切图为初始化，而非永久冻结边界。固定切图时对所有拓扑合法的子图映射/顺序进行增量或局部搜索；候选生成必须遵守官方 plan 约束。对输入按消费核去重，对跨核张量按 `(tensor, source_core, target_core)` 去重；同时识别官方评估器中逐边插入的直接跨核依赖，核内数据驻留和输出写回。估计每核 Pipe 负载、就绪时刻、关键路径等待、共享 DDR 排队和缓存峰值。**以官方 makespan 作为最终选择目标**，估计模型仅负责缩小搜索空间，不用未经验证的固定线性代价式替代官方评估。
3. **核内排序与内存。** 在映射相同的配对实验中，从合法就绪子图中选择执行次序，联合考察释放旧张量、新增长寿命张量、后继关键性和跨核消费时刻。每次变更执行官方合法性检查；Spill 是观测和优化信号，但不把“零 Spill”当最终目标。若高 Spill 源自切图边界或生命周期，而排序无效，允许有预算的局部切图修补；不得破坏问题一求解器。
4. **候选管理与消融。** 建立独立的问题二候选管理器，复用安全的缓存、去重、择优思想，不复用场景 A 的评分。最低限度比较 `single` 加五个非 single 切图在“原映射 / 场景 B 映射”下的 11 个逻辑候选；排序变体和低核继承单列消融，确保同切图、同预算、同评估口径。缓存键应包含图、配置、候选方案、评估器及实现版本；跨环境不可信缓存必须失效。记录所有尝试和失败，而不只保存赢家。
5. **结果与论文支撑。** 用训练/调参 case 与最终汇报 case 分离的方式降低过拟合；报告全量 100 case、1–5 核的平均加速比曲线、运行时间、尾部退化、搬运与 Spill 的权衡、显著性/稳健性。与去年的常规“内存感知就绪调度”区分：创新叙述聚焦多核映射、同步关键路径、共享 DDR、核内驻留的联合权衡，并做正式文献核对，不把贪心/模拟退火本身称为创新。

实现要求：尽量新增问题二模块和测试，不改官方评估器及原问题一核心行为；先做小规模配对实验，再扩大到全量。每个阶段提交给我：改动文件、准确运行命令、指标表、失败案例、消融结论和下一步判断。若发现题目接口或官方语义与以上理解不符，以源码为准，明确报告差异后再调整设计。
